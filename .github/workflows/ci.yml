name: CI

on:
  push:
    branches: [main]
  pull_request:

# A second push to the same branch cancels the first — no point finishing a
# build for a commit that's already been superseded.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - run: npm ci

      # PageProps/LayoutProps are generated into .next/types, so a fresh
      # checkout has to run typegen before tsc can see them.
      - name: Types
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      # Build last: it's the slowest and the least specific about what broke.
      - name: Build
        run: npm run build
